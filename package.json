{
  "name": "whatsapp-ai-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for WhatsApp AI Bot with Mistral integration",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.7.5"
  }
}
